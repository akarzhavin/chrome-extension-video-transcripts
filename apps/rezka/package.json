{
  "name": "@video-transcripts/rezka",
  "version": "1.0.13",
  "private": true,
  "scripts": {
    "build": "vite build --mode background && vite build --mode content && vite build --mode interceptor && vite build --mode popup && npm run zip",
    "build:dev": "EXT_ENV=dev vite build --mode background && EXT_ENV=dev vite build --mode content && EXT_ENV=dev vite build --mode interceptor && EXT_ENV=dev vite build --mode popup",
    "zip": "npm run assert-shippable && mkdir -p ../../releases && cd build && rm -f ../../../releases/rezka-v$npm_package_version.zip && zip -r ../../../releases/rezka-v$npm_package_version.zip .",
    "assert-shippable": "node ../../packages/shared/assert-shippable.mjs build --label rezka",
    "dev": "vite",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@video-transcripts/shared": "*"
  }
}
