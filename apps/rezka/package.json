{
  "name": "@video-transcripts/rezka",
  "version": "1.0.2",
  "private": true,
  "scripts": {
    "build": "vite build --mode background && vite build --mode content && vite build --mode interceptor && npm run zip",
    "zip": "mkdir -p ../../releases && cd build && zip -r ../../../releases/rezka-v$npm_package_version.zip .",
    "dev": "vite",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@video-transcripts/shared": "*"
  }
}
