{
  "name": "@video-transcripts/youtube",
  "version": "1.0.22",
  "private": true,
  "scripts": {
    "build": "../../scripts/build-with-analytics.sh prod youtube",
    "build:dev": "../../scripts/build-with-analytics.sh dev youtube",
    "build:capture": "../../scripts/build-with-analytics.sh capture youtube",
    "build:raw": "npm run build:vite:raw && npm run zip",
    "build:vite:raw": "vite build --mode background && vite build --mode content && vite build --mode page-script && vite build --mode popup",
    "build:dev:raw": "EXT_ENV=dev vite build --mode background && EXT_ENV=dev vite build --mode content && EXT_ENV=dev vite build --mode page-script && EXT_ENV=dev vite build --mode popup",
    "zip": "node ../../packages/shared/zip-build.mjs --label youtube --version $npm_package_version",
    "dev": "vite",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@video-transcripts/shared": "*"
  }
}
