{
  "name": "@video-transcripts/youtube",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "vite build --mode background && vite build --mode content && vite build --mode page-script && npm run zip",
    "zip": "mkdir -p ../../releases && cd build && zip -r ../../../releases/youtube-v$npm_package_version.zip .",
    "dev": "vite",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@video-transcripts/shared": "*"
  }
}
