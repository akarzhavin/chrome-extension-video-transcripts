# 2026-08-27 — store-en@5 slide 3, panel-crop version

The guess-mode slide as `store-en@5` renders it: copy on the pale ground beside
a crop of the sidebar panel. Displaced on 2026-08-27 when `promo-3.png` was
replaced by hand with the fullscreen treatment
(`out/fullscreen@1/fullscreen-guess.png`) — same message, different framing.

Kept because `out/` is gitignored and every pipeline overwrites its own subtree.
This one IS reproducible from source (`node pipelines/store-en@5/render.mjs`),
unlike the rest of `archive/` — but only for as long as the guess capture stays
in guess state, and that has already been lost once (see the pipeline's history).

To go back, re-render the pipeline — or just copy these two files into
`out/store-en@5/`.
