{
  "name": "store-i18n",
  "version": "5.0.0",
  "title": "Localized Chrome Web Store screenshot series, all locales (v5, 4 slides)",
  "style": "v5 — the same four slides as store-en@5, rendered from JSON copy instead of static HTML: 1 fullscreen hero, 2 personal dictionary, 3 guess/active recall, 4 customisation.",
  "run": "node pipelines/store-i18n@5/render.mjs [-j N] [locale...]",
  "sourceOfTruth": "assets/promo-copy.json — 54 locales, English is the per-field fallback",
  "inputs": {
    "assets": [
      "assets/promo-copy.json",
      "assets/promo.css",
      "assets/brand-tile.png",
      "assets/save-slide.css",
      "assets/fullscreen-panel.css",
      "assets/fullscreen-slide.css",
      "assets/fullscreen-warm.css",
      "assets/fullscreen-backdrop.jpg",
      "assets/fullscreen-panel-guess.css",
      "assets/fullscreen-slide-guess.css",
      "assets/fullscreen-dark.css",
      "assets/fullscreen-guess.css",
      "assets/fullscreen-backdrop-guess.jpg"
    ],
    "captures": {
      "dir": "../../../screenshots/out-live",
      "files": [
        "live-demo-<locale>.png",
        "live-demo-guess-<locale>.png",
        "live-demo-settings-<locale>.png"
      ],
      "recapture": "node apps/youtube/screenshots/capture-backdrop.mjs --locale <loc> --learn es --native <loc> --modes sidebar,guess,settings --theme-pref light",
      "note": "Resolved per locale at run time, falling back to the English capture when a locale has not been captured. Only the English fallbacks are required up front. Capture from a PROD build (plain `vite build`, no EXT_ENV=dev) — a dev build paints a lime \"backend: env\" bar across the panel. IMPORTANT: the extension UI inside a capture is localized by demo mode's i18n override, NOT by Chrome's --lang (which does not change extension UI language). That override is generated by screenshots/gen-demo-ui.mjs from _locales/; run it and REBUILD before capturing, or the settings panel comes back in English while the rest is localized."
    },
    "assetsNote": "Two panel sheets, deliberately. Slide 1 uses fullscreen-panel.css (store-en@5's copy: enlarged captions, no sidebar glow); slide 3 uses fullscreen-panel-guess.css (fullscreen@1's copy: carries the .panel::before violet glow the guess frame needs). They are near-identical and it is tempting to merge them — don't: rendering slide 3 with the slide-1 sheet silently drops the glow, which is a 79px-wide difference nothing else catches."
  },
  "outputs": {
    "dir": "out/store-i18n@5",
    "files": [
      "<locale>/screenshot-<n>.png (1280x800), n = 1..4"
    ],
    "intermediates": "shots/store-i18n@5",
    "note": "The CWS autofill snippet reads the language from the folder directly containing screenshot-N.png, so <locale>/ must stay the last path segment. Point that snippet at out/store-i18n@5/. The series went 5 -> 4 in v5: a stale screenshot-5.png from an earlier run would be uploaded as a fifth slide showing the old design, so the renderer's output dir must be cleared of them."
  },
  "knownIssues": [
    "Product-UI strings drawn inside the rebuilt panel (its title, the mode chips, \"N words saved\") come from apps/youtube/_locales/<loc>/messages.json, read at render time — NOT from promo-copy.json. That is deliberate: a screenshot must never show a mode name the product does not use. It also means the panel text changes when _locales changes, with no re-render of the copy file needed.",
    "The ORIGINAL transcript lines in the panel are Spanish in every locale (Spanish is the language being learned throughout the series); only the translation rows follow the locale. Slide 1 shows 8 such rows and slide 3 shows 2 — the rest stay masked by guess mode.",
    "slide 2's overlay is positioned at RENDER time by placeOverlay() in render.mjs, which finds the lime active card in the capture and offsets from its top edge. It has to: the card sits at a different height per locale because a translation row that wraps to two lines pushes it down (measured y 371 for en/ru/de, 394 for id/sr/vi, 401 for kn/ta). The values it writes are PRE-SCALE — .stage .panel carries transform: scale(1.07), so rendered pixels must be divided by 1.07 before being written to style.top. Writing rendered pixels straight back moves the overlay 7% too far and silently breaks English, which was previously correct."
  ],
  "verify": {
    "run": "KEEP_BUILD=1 node pipelines/store-i18n@5/render.mjs   # then the three checks below",
    "check-overlay.mjs": "slide 2: the selection swatch must sit on \"vamos\", and every capture must be no older than the English one. Reads the OUTPUT png, not the build html — the position is applied by script after layout. Discriminates by the swatch's distance below the active card's top edge (21px, calibrated on English): ink alone does not work, because a swatch one row too high lands on the previous subtitle line and measures MORE ink (18.6% vs 16.6%).",
    "check-overflow.mjs": "no headline, paragraph, eyebrow, callout or caption may leave the 1280x800 canvas.",
    "check-orphans.mjs": "no headline may end with an orphan — a last line under 25% of the widest. Line COUNT is not the test: ru slide 3 wraps to 5 lines and reads fine."
  }
}
