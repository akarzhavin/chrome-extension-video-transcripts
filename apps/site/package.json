{
  "name": "@video-transcripts/site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs && vite build && vite build --config vite.auth.config.ts",
    "dev": "node build.mjs --watch",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@video-transcripts/embed": "*",
    "firebase": "^11.0.0"
  }
}
